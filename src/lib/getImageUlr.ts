// export function GetImageUrl(path: string){
//     return `http://localhost:3333/files/${path}`;
// } ///Atencao, mesmo que o nosso frontend esteja sendo carregado no localhost 3000, o link da nossa imagem precisa ser na porta 3333 que é a mesma do nosso backend.